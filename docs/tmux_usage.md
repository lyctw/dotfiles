Capture buffer cotents

<prefix> + `:` `capture-pane -b temp-capture-buffer -S -`      # Capture
               `save-buffer -b temp-capture-buffer ~/tmux.log` # Store
               `delete-buffer -b capture-buffer`               # Clear
